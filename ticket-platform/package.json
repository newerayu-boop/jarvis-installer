{
  "name": "ticket-platform",
  "version": "1.0.0",
  "description": "Telegram bot that generates branded event tickets (name + number in, PNG out) with QR check-in",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "preview": "node scripts/preview.js",
    "postinstall": "playwright install chromium"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "express": "^4.22.2",
    "playwright": "^1.61.1",
    "qrcode": "^1.5.4",
    "telegraf": "^4.16.3"
  }
}
