{
  "name": "ticket-platform",
  "version": "2.0.0",
  "description": "Telegram bot that generates branded event tickets (name + number in, PNG out). Renders with Satori + resvg — runs on Vercel or any Node host.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "preview": "node scripts/preview.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "dotenv": "^16.4.5",
    "satori": "^0.26.0",
    "telegraf": "^4.16.3"
  }
}
